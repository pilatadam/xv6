#include "user.h"
#include "kernel/param.h"

int
main(int argc, char *argv[])
{

  char *argv_arr[MAXARG];


  // read from stdin until '\n'
  // run command with fork + exec
  // wait in the parent for the end of execution
  // repeat

  exit(0);
}
